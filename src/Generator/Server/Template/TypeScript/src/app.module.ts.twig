import { Module } from '@nestjs/common';
{% for path, controller in controllers %}
import { {{ controller }} } from './controller/{{ path }}.controller';
{% endfor %}

@Module({
  imports: [],
  controllers: [{% for controller in controllers %}{{ controller }}, {% endfor %}],
  providers: [],
})
export class AppModule {}
