{% if namespace %}
namespace {{ namespace }};
{% endif %}

use Sdkgen\Client\Exception\KnownStatusCodeException;

class {{ className }} extends KnownStatusCodeException
{
    private {{ schema.type|raw }} $payload;

    public function __construct({{ schema.type|raw }} $payload)
    {
        parent::__construct('{{ message }}');

        $this->payload = $payload;
    }

    /**
     * @return {{ schema.docType|raw }}
     */
    public function getPayload(): {{ schema.type|raw }}
    {
        return $this->payload;
    }
}
