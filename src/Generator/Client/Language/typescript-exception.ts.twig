import {KnownStatusCodeException} from "sdkgen-client"

{% for file, className in imports %}
import {{ '{' }}{{ className }}{{ '}' }} from "./{{ file }}";
{% endfor %}

export class {{ className }} extends KnownStatusCodeException {

    public constructor(private payload: {{ schema.type|raw }}) {
        super('{{ message }}');
    }

    public getPayload(): {{ schema.type|raw }} {
        return this.payload;
    }

}
